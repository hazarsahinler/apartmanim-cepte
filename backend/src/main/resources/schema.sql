-- src/main/resources/schema.sql dosyasının içeriği

CREATE SCHEMA IF NOT EXISTS identity;
CREATE SCHEMA IF NOT EXISTS billing;   -- Örnek: faturalandırma için yeni bir şema
CREATE SCHEMA IF NOT EXISTS logging;   -- Örnek: loglama için başka bir şema
-- İhtiyaç duyduğunuz diğer tüm şemaları buraya ekleyebilirsiniz.